# File Map

```
RestOfIryna/
├── .env.example                    # Environment template (TG token, DB creds)
├── .gitignore                      # Ignores .build, .env, .xcodeproj, etc.
├── .memory/                        # Project-scoped AI memory. INDEX.md links all; content-pipeline.md (data-driven catalogs) and rebalance.md (active work: decisions + calibrated math + phase tracker) were added 2026-08-29
├── Package.swift                   # SPM manifest, Swift 6.2, macOS 14+ — 5 targets (see Modules/)
│
├── Modules/                        # Content pipeline (added 2026-08-29, rebalance Phase 0)
│   ├── ROIContent/                 # library, Foundation ONLY — no Fluent/Telegram, so the CLI + tests build in ~1s
│   │   ├── ContentSchema.swift     # `current` schema version; binary↔bundle handshake
│   │   ├── ContentError.swift      # Designer-readable failure modes
│   │   ├── ContentBundle.swift     # Decoded-but-unvalidated directory + summaryLine
│   │   ├── ContentLoader.swift     # Reads content/data/*.json; renders DecodingError as "file · path · id · expected"; FNV-1a content hash; NEVER sorts (pickFor order is load-bearing)
│   │   ├── GameContent.swift       # Immutable validated snapshot; all lookup dicts built once; dedupes with uniquingKeysWith (uniqueKeysWithValues TRAPS)
│   │   ├── GameData.swift          # nonisolated(unsafe) + NSLock holder; keeps catalog façades sync/non-throwing for ~315 call sites. NOT @TaskLocal (doesn't cross Task.detached — 6 detached tasks read catalogs)
│   │   ├── LocaleIndex.swift       # en/uk flat maps; `has()` accepts uk `.m`/`.f` pairs (21 keys rely on this); emoji-before-%{} detector
│   │   ├── DTO/                    # ItemDTO · EnemyDTO · RecipeDTO · ManifestDTO · WeaponUpgradeDTO · UpgradeDTO (bag + estate, sharing MaterialCostDTO) — hand-written init(from:) because Swift ignores property defaults for missing keys
│   │   └── Validation/             # ContentIssue/ContentReport + ContentValidator (identity · enums · references · localization · timeScale)
│   ├── ROISim/                     # library → ROIContent
│   │   ├── SplitMix64.swift        # Seedable RNG + OutcomeDigest — the migration equivalence proof needs reproducible rolls
│   │   └── ROISim.swift            # Simulator namespace (model lands Phase 8, after CombatantStats)
│   └── roi-content/main.swift      # CLI: validate (exit 0/1, CI-ready) | simulate (Phase 8)
│
├── Tests/
│   └── ROIContentTests/            # 37 tests: DTO defaults/round-trip, validator rules, ladder integrity + tier-aware locale keys, LocaleIndex gendered keys + emoji rule
├── GDD.md                          # Game Design Document (full v1 vision)
├── README.md                       # Project overview, arch, setup, dev notes
├── CLAUDE.md                       # AI assistant instructions & project reference
├── TODO.md                         # Phased progress tracker
├── Prompt.md                       # New-session primer
├── icon.png                        # Bot icon asset
│
├── Localizations/
│   ├── en.json                     # English strings (956 keys as of 2026-08-23)
│   └── uk.json                     # Ukrainian strings (977 keys — +21 from the gendered `.m`/`.f` pairs)
│
├── Assets/
│   ├── registration/               # Artwork for the onboarding narrative
│   │   ├── kings_charter.jpg       # Shown in the King's Oath step (same for every class)
│   │   └── <class>_estate_<m|f>.jpg # Rabid-dog encounter step (player's first look at the estate). 6 files: warrior/archer/mage × m/f. Path built by `CharacterClass.journeyImageName(gender:)`. No genderless fallback (removed 2026-05-21).
│   └── estate/                     # Per-tier estate artwork — `level_<N>.jpg`, N = estateLevel 1…7 (EstateUpgradeCatalog.maxTier). Loaded directly by EstateController.showEstate; all 7 present (2026-05-21).
│       └── level_1.jpg … level_7.jpg
│
├── Public/
│   └── favicon.ico                 # (if present)
│
├── content/
│   ├── data/                       # DATA-DRIVEN CONTENT (exported 2026-08-29, rebalance Phase 1)
│   │   ├── manifest.json           # schemaVersion + contentVersion + timeScale (60.0 while the three testMode flags are on)
│   │   ├── items.json              # 33 items — locale keys omitted where derivable (`item.<id>`); explicit null = no description key
│   │   ├── enemies.json            # 9 enemies — depthRange exported verbatim, `0...0` sentinel included, nothing normalized
│   │   ├── recipes.json            # 12 recipes + starterRecipeIds (sorted; the source is an unordered Set)
│   │   └── weapon_upgrades.json    # 3 ladders × 5 tiers + durabilityByTier; explicit `tier` field so an off-by-one can't shift a ladder silently
│   │                               # NOTHING READS THESE YET — the bot still runs off the Swift arrays until Phase 2.
│                                   # Reference documents (game content authoring)
│   └── bestiary.md                 # Per-enemy stats, loot, depth ranges, family overviews
│
├── PostgreSQL/                     # Docker volume mount for PG data
│
└── Swift/                          # All source code (SPM target root)
    ├── entrypoint.swift            # @main, logging setup, calls configure()
    ├── configure.swift             # App bootstrap: DB, Lingo, Bot, Hummingbird
    ├── routes.swift                # RouterStore actor + Sendable conformance + per-user dispatch serialization (token-keyed Task chain)
    │
    ├── Controllers/
    │   ├── AllControllers.swift     # Controller registry, attachAllHandlers()
    │   ├── MainController.swift    # Main hub, profile view (3 styles, with Phase 5.3a XP fragment per style — compact `📊 XP X/Y`, text bar `📊 ████░░ X/Y`, emoji bar `🟦` block; renders `Max` at L21 since `User.xpToNextLevel` returns Int.max), Explore/Estate/Capital/Profile/Settings nav. **Phase 9.2 (2026-08-23)**: profile keyboard gained a second row `[📓 Нотатник]` (`journal:open`) → `showJournal` edits the SAME profile bubble into a read-only digest of today's three capital jobs (NPC label reused from `capital.button.<npc>`, per-job state ✅ claimed / 🎁 ready / ⏳ done/target + reward, `🕛 resets in Xг Yхв` via `GameDay.secondsUntilNextRollover`, hint that turn-in happens at the NPC); `journal:back` edits it back to the profile. Nothing is claimable from the journal — it can't become a remote control for the capital. Reward wording delegates to `CapitalController.rewardPhrase` so board + journal can't disagree. Title is gendered (намісника/-иці) → gender overload.
    │   ├── RegistrationController.swift  # Multi-step: language, nickname, class, estate. First message ships ReplyKeyboardRemove. Shared validateName helper: allow-lists (digits/Latin/Ukrainian), edge-space + consecutive-space + invalid-char rejection with five distinct error toasts per field.
    │   ├── SettingsController.swift      # Language change, back navigation
    │   ├── GlobalCommandsController.swift # /help, /settings, /buttons (any state)
    │   ├── ExplorationController.swift   # Phase 3.1 active + 3.2 visit-decay return + 3.3 passive mode. Entry shows mode picker [🏃 Розвідка / 🏕 Експедиція] when no state; passive branches into countdown status (in-flight) or report delivery (completed). Active: Step Forward increments & rolls with prior visit count; Step Back decrements & rolls (km ≥ 2) or arrives home (km ≤ 1). Reply keyboard [🚶 Step fwd] [🔙 Step back] / [🎒 Bag] only during active. Inline `explore:` callbacks for bag, mode-picker, duration-picker, passive-report close. Encounters in active mode hand off to CombatController via `handOffToCombat` (stamps combat fields on state row, transitions routerName). `handleDeath(causeNarrative:)` is now a static helper so CombatController can reuse the wipe + respawn flow.
    │   ├── CombatController.swift        # Phase 4.1 + 4.2 + 4.3.1 + 5.1 turn-based PvE duel. **Reply-keyboard driven (2026-05-27 switch from inline).** Main keyboard `[Attack][Defend] / [🪄 Techniques][Flee]` is a `TGReplyKeyboardMarkup` (`combatReplyKeyboardMarkup`/`combatReplyKeyboard`) that REPLACES the main keyboard for the fight; actions match by button text registered per class × locale in `attachHandlers` (onAttack/onDefend/onFlee/onSpecialAttack/onSpecialDefense/onSuper/onTechMenu/onTechBack/onTrainingExit). `generateControllerKB` returns the default (non-training) combat keyboard for `/buttons`. `[🪄 Techniques]` (onTechMenu) sends a FRESH message with a sub-keyboard of only usable techniques (learned AND uses>0) — unlearned/spent hidden — with `× N` in the body (labels stay static so routing matches); `[🔙 Back]` (onTechBack) re-sends the status card + main keyboard; when none usable, re-sends main keyboard + `combat.tech.none_available`. The static `onCallbackQuery` is now a LEGACY fallback only — recovers pre-switch inline buttons lingering in chat (answers the spinner, re-presents the fight, flips routerName→combat if forwarded). `loadCombat` stale path + `sendLockedToastIfUnlearned` use plain messages instead of modal toasts. Phase 4.3.1: per-class Flee chances (warrior 40 / archer 70 / mage 90, mage +2 vigor teleport tax). **2026-05-15 per-class basic Defend**: warrior keeps the canonical chip 30% × ATK + 2× DEF; archer gets chip 15% (knife flick while hiding) + flat +30 dodge for the round, DEF stays single; mage skips chip entirely and the landed enemy hit is multiplied by 0.4 (60% damage reduction) after applyAttack rolls normally. Renders `combat.defend.barrier` line for the mage instead of `combat.defend.absorbed` since there's no chip to interpolate. **Phase 5.1 Training Mode** (entered from the Training Ground plot): detected via `isTraining(state)` (enemy id check on `enemy.training_dummy`); the trailing button is `[🚪 Exit]` (`combat.button.training_exit`, renamed from `[🔙 Back]` 2026-05-27 to avoid a text collision with the techniques `[🔙 Back]`); `EstateController.handleTrainingSpar` now flips `routerName` to "combat" (was "estate") so the reply-keyboard text buttons reach this controller, and `onTrainingExit` restores `routerName="estate"` + the main keyboard (attached to the exit banner) before re-rendering the Plot list. Player swings are clean (cannotMiss=true, enemy DEF treated as 0 via `playerSwingEnemyDEF` helper), no vigor drain, no enemy counter line, dummy auto-revives via `reviveTrainingDummy` when HP hits 0. Static emoji helpers (superEmoji / specialAtkHitEmoji / specialDefEmoji) prepend icons in Swift since Lingo's %{var} parser breaks on leading UTF-16 surrogate pairs. Registration rabid-dog fight routes here — Registration.handleCombatEnd(won:) on end-of-fight, detected via session.registrationStep < User.registrationDoneStep.
    │   ├── EstateController.swift        # Tree nav: Root → House (real Workshop + real Kitchen + real Warehouse) / Plot. Per-level artwork loader. Main-nav pass-through. Warehouse deposit/withdraw success → inline `✅ ...` status line above refreshed category; failure → modal alert. **Phase 5.1 Plot drill-down**: `renderPlotList(plots:session:)` and `plotListKeyboard(...)` are `internal` (CombatController.onTrainingExit re-renders them after a Back tap). Three callback handlers: `handlePlotClaimPicker` opens a 5-type picker (Farm / Lumberyard / Mine / Coop / Training Ground), `handlePlotTypeChosen` calls `PlotService.claim(...)`, `handlePlotHarvest` calls `PlotService.harvest(...)` and deposits into Warehouse with a `✅ Slot N — yields, added to Warehouse 📦` status banner, `handlePlotTraining`/`handleTrainingSpar` spawns an `ExplorationState` row with `enemy.training_dummy` stamped on combat fields, flips `routerName` to "combat" (2026-05-27 — combat now owns a reply keyboard; was kept at "estate" before), and hands off to CombatController. The `combat:*` callback forwarding in EstateController/MainController/etc. is now just a stale-button safety net. Mine plot's bonus output (iron alongside pebble) renders inline in the plot row: `⛏ Slot N · Mine — 40/40 🪨 · 12/20 🔩`. **Phase 5.2 Workshop (two-screen)**: `renderWorkshop` + `workshopKeyboard` build the compact outer list (title + atmospheric description + one `[<icon> <name>]` button per recipe + Back) — no in-body category headers, recipes are visually grouped through their declaration order in `RecipeCatalog.all`. `renderRecipeDetail` + `recipeDetailKeyboard` build the per-recipe detail screen (output icon + name, lore description from `descriptionKey`, `📜 Recipe` inputs, `📊 Stats` for gear outputs — only non-zero stats shown with per-stat icons prepended in Swift). Static handlers `handleCraftDetail` (opens detail) and `handleCraft` (performs craft, refreshes detail with the `✅ Crafted ...` banner appended **at the bottom** of the body so it stays visible without scrolling). Failures surface as modal alerts (missingMaterials lists each shortage row; inventoryFull asks the player to free a slot) and leave the screen unchanged. The dispatcher routes `craft:detail:` and `craft:` callbacks **before** the `estate:` prefix guard so they don't fall through to the unsupported-content reply. **Phase 5.2.1 Kitchen** mirrors the Workshop UI: `renderKitchen` + `kitchenKeyboard` (compact list, only **learned** kitchen recipes shown; empty-state hint when nothing learned), reuses `renderRecipeDetail` (extended to render a `📊 Effects` section listing each `ItemEffect` with a per-effect icon — 🍖 Vigor / ❤️ HP) + `recipeDetailKeyboard` (action verb and back target both come from `recipe.category`). `handleCraftDetail` and `handleCraft` enforce the learn-set on every kitchen recipe id (defensive against stale callbacks). **2026-05-15 Warehouse `[✏️ N]` flow**: stackable warehouse rows now render as TWO inline rows — `[icon name · 🎒N / 📦M]` (info modal) above `[⬆️ +1] [⬇️ +1] [✏️ N]` (deposit-1 / withdraw-1 / open custom-quantity prompt). Gear rows unchanged. The `[✏️ N]` button (callback `estate:wh:transferN:<id>`) opens a two-stage flow: stage 1 is a direction picker (`[⬆️ To warehouse] [⬇️ To bag]` inline buttons on a fresh message + `[❌ Cancel]` below), stage 2 edits that prompt in place into a quantity question once the player taps a direction. Four static handlers: `handleWarehouseTransferNEntry` (stage 1 — sends the picker message, records pending with direction=nil), `handleWarehouseTransferDirection` (`transferPut:` / `transferTake:` — bumps direction on the pending entry, edits prompt to "How many to deposit/take?" with just `[❌ Cancel]` left), `handleWarehouseTransferNCancel` (works in either stage — clears pending, deletes prompt), `handleWarehouseTransferNText` (consumed by `unmatched()` only when `pending.direction != nil` — parses the typed number, calls `WarehouseService.withdrawN` for `.take` or `WarehouseService.depositN` for `.put`, three branches per direction: invalid input → edit prompt with error, keep pending; quota failure → delete prompt, refresh warehouse with `❌ …` banner carrying the specific limit; `.success` → delete prompt, refresh with `✅ item ×N — taken/deposited` banner). Callback dispatch groups `transferN:` / `transferPut:` / `transferTake:` / `cancelN` ahead of the generic `withdraw:` / `deposit:` prefixes.
    │   ├── CapitalController.swift       # Phase 6.0 + 6.1 + 6.2 + 6.4: travel-gated city hub. Reply-keyboard with 6 location buttons (Market / Arena / Trader / Fortune Teller / Master / Tavern) + utility row [Inventory] [Profile] + 🏡 Back-to-estate. `showCapital` is the single entry from MainController/EstateController/InventoryController; branches on expedition→block / travel→countdown / at-estate→start trip / at-capital→render welcome. Stub locations (Market / Arena / Master) go through `renderLocation(_)` which auto-loads `Assets/capital/<id>.jpg` if present. Static `beginTrip(destination:, context:)` is the shared trip-start orchestrator used by `showCapital`, `onLeave`, and `EstateController.showEstate`; flips routerName to main + "you set out" render + per-error banners. **Trader (Phase 6.1 + 2026-05-18 flatten + category split)**: `showTrader` sends `Assets/capital/trader.jpg` + caption with `[💰 Купити][💸 Продати]`. `editToBuyList`/`editToSellList` now render a category picker `[🥩 Їжа][🪨 Матеріали] + [🔙 До крамаря]` (Inventory-style "categories first"). New `editToBuyItems(category:ItemType)` / `editToSellItems(category:)` render the filtered single-button-per-item lists `[<icon> <name> · 🪙 <price>]` / sell side adds `🎒 <qty>`. Tap on an item button opens the bulk-N prompt directly (no info modal, no ×1 button — removed in the flatten). Callbacks: `trader:buy:food` / `trader:buy:materials` / `trader:sell:food` / `trader:sell:materials` (with `traderCategory(from:slug)` static mapper); `trader:buyN:<id>` / `trader:sellN:<id>` for the prompt; `trader:cancelN`. `handleTraderBulkInput` after action derives category from `ItemCatalog.find(itemId)?.type` (defensively defaults to `.material`) so the refresh stays on the same category screen. `postTraderResultBanner` attaches `backToCapitalBannerKB(lingo:locale:)` inline kb to every banner — player never gets stuck when the photo bubble has scrolled off. **Tavern (Phase 6.2)**: `showTavern` sends `Assets/capital/tavern.jpg` + `[🍲 Меню][🎲 Кості][🎯 Влучанка]`; food menu / wager screens share `editTraderScreen` helper. Wager flow: tap stake → "Готовий?" + `[🎲 Кинути кубики][❌ Скасувати]` (no silver debited yet — free cancel) → tap Roll debits + runs the bot-rolls-both-sides sequence with sleeps. Banner reply-kb anchor on `postTavernBuyBanner` + mid-roll insufficient-silver banner. **Fortune Teller (Phase 6.4)**: 22 Major Arcana draw for 10🪙 → 6h buff window + 24h cooldown (split via separate `lastFortuneDrawAt`). Three render states (can-draw / cooldown-with-buff / cooldown-only). Reveal sends card art (`Assets/capital/fortune/<id>.png`) via `sendCachedPhoto` + meaning + buff desc + one-shot deltas (e.g. "+🪙 30 · balance 145") + countdown. Active card surfaces in profile via the fortune line. **`onCallbackQuery` forwards unknown callbacks (`pstyle:`, stale `explore:`/`combat:`) to `MainController.onCallbackQuery`** instead of returning false — quiet handling of stale buttons. Cross-controller `inv:*` forwarding kept so inventory drill-down works while routerName stays "capital". **Master (Phase 6.5)**: `editToMasterMenu`/`Buy`/`Repair`/`Enchant` lists; each item tap opens a confirm screen (`editToMasterConfirm{Buy,Repair,Enchant}` + `masterConfirmKeyboard`) before acting — `master:buy:/repair:/enchant:` show the prompt, `master:*ok:` execute via `MasterService`. Buy list keeps the price; repair shows durability; enchant shows the level step. **Market (Phase 6.5, 2026-05-28)**: `onMarket` → `showMarket` (photo + silver + lot count + `[🛒 Buy][🏷 Sell]` / `[📦 My lots]` / back). Two-level item-grouped buy board — `editToBuyBoard` (Level 1, `MarketListing.itemSummaries` excluding own, `market:item:<id>`) → `editToItemLots` (Level 2, `forItem` minus own, cheapest-per-unit first, `market:buy:<lotId>`) → `editToBuyConfirm` (`market:buyok:<lotId>` runs `MarketService.buyListing` + `postMarketBuyBanner` + `notifySellerSold` push, refreshes board). `editToMyLots` (`market:cancel:<lotId>` → `cancelListing`). Sell: `editToSellPicker` (bag stackables) → `openMarketSellPrompt` two-stage (`PendingMarketListing` qty→price, fee shown in price prompt) consumed by `unmatched` → `handleMarketListingInput` → `createListing`; `market:cancelN` cancels. All `market:*` callbacks dispatched in `onCallbackQuery` ahead of `capital:back`. **Trade / Обмін (Phase 6.5, 2026-06-10) — UI side** drives `TradeStore`/`TradeService`: `trade:lobby` (presence list) → `trade:invite:` → `trade:accept:`/`trade:decline:` cross-user push → `buildBuildingScreen` (bag toggles `trade:stack:`/`trade:gear:`, `trade:silver` prompt) → `trade:ok` (`handleTradeConfirmFirst`) → `buildLockedScreen` (combined offer) → `trade:final` (`handleTradeConfirmSecond` → commit). **2026-06-15 post-playtest polish:** (1) `finishTradeSuccess` posts a PERMANENT per-side record (✅ done + with-whom + gave/got via `capital.trade.gave`/`got`) as a fresh message at the BOTTOM of the chat (a kept history of when/with-whom), then restores the Market menu — replaces the plain done-banner for the success case only. (2) Bazaar message-visibility policy: transient numeric prompts (market listing qty→price, trade silver/qty) are DELETED on submit/cancel/teardown; ✅/❌ banners + the trade record + menu screens are KEPT.
    │   ├── InventoryController.swift     # Tree nav: root categories → drill-down with Use buttons for food/potion. `refreshCategory(...)` accepts optional `statusLine` prepended to the rendered body. Action acks (eat, equip, unequip) → silent answerCallbackQuery + inline `✅ ...` status above refresh; warnings (raw food, no_effect, empty category, use_unavailable) and item-info placeholders → modal alert via showAlert: true. Eat status appends current/max pool ("✅ Лісові ягоди — +15 голоду (20/100)") via `vigor.restored` / `hp.restored` keys interpolating `%{current}`/`%{max}`. **Phase 5.2.1 Learn flow**: `genericRows` overrides the artifact action label to "📖 Learn" when `item.teachesRecipe != nil`; the same `inv:use:<itemId>` callback branches in the dispatcher to `handleLearnRecipe` which calls `LearnedRecipe.add`, deletes the scroll on a fresh learn, and shows an `✅ <Dish> — recipe learned` banner above the refreshed Artifacts category. Already-known recipes leave the scroll alone and surface a `📖 You already know this recipe.` modal alert. **Phase 6.5 (2026-05-22)**: tapping a gear item sends a full HTML detail card as its own message via `gearDetailCard(...)` (name+tier, lore, full-condition stats from `EquipmentService.nominalStats`, condition block: durability dur/max, broken/dulled warning, enchant level); non-gear keeps the lore alert. Gear-row labels show only a plain `+N` enchant (no ✨ icon); durability no longer crammed into the button.
    │   └── GuildController.swift         # Phase 7.1 (2026-06-16): capital Guildhall. Entered via CapitalController's `🏰 Гільдії` button (flips routerName to "guild"); owns its reply keyboard, branched on membership — guildless [➕ Found][📜 List]/[🔙], in-guild [👥 Members][🏦 Vault]/[🪙 Treasury][⚙️ Manage]/[🔙]. Top-level nav = reply buttons; drill-downs = inline + text prompts (`EphemeralChatState.PendingGuildInput`, kinds foundName/inviteName/vaultDeposit(itemId)/vaultWithdraw(itemId)/treasuryDeposit/treasuryWithdraw). Callbacks `guild:*` (cancelfound/leave/disband/invite/managemembers/acceptinv:/declineinv:/kick:/promote:/demote:/vault/vaultdeposit/vaultwithdraw/vdep:/vwd:/treasury/treasdeposit/treaswithdraw); unknown → MainController.onCallbackQuery. `tagSuffix(_)` renders ` [TAG]` or empty (tag is creator-assigned via DB, empty on found). All logic in GuildService; cross-player notices are fire-and-forget pushes.
    │   └── ArenaController.swift         # Phase 8.3 "Ристалище" (2026-07-20): live real-time PvP duel (герць). Entered via CapitalController's `⚔️ Ристалище` (flips routerName to "arena"). One reply keyboard, guarded by whether a live duel exists: hub `[⚔️ Виклик][🏆 Честь]/[🔙 Столиця]` vs fight `[⚔️ Атака][🛡 Оборона]/[🏳 Здатися]`. Fight keyboard stays up the whole duel; `ArenaStore.resolve` rejects out-of-turn taps (postStatusBanner). Flow: `showArenaHome` touches lobby + renders hub (`ArenaProfile`). Challenge: `onChallengeTapped` → lobby list (`arena:chal:<tg>`) → `showStakePicker` (`arena:stake:<tg>:<amt>`) → `issueChallenge` (validateMatch → `ArenaStore.challenge` → push invite `arena:acc:`/`arena:dec:` to target). Accept: re-validate → snapshot both fresh → `ArenaStore.startDuel` → `pushDuelState` to both. Fight: `handleAction(.attack/.defend)`/`onSurrenderTapped` → `ArenaStore.resolve`/`surrender` → continued=`pushDuelState`, ended=`ArenaService.settle`+`pushDuelResult`. `onHonor` = rating/league/W-L/daily + top-10. Leaving (back/`/start`) forfeits any live duel + cancels any pending challenge. Static render helpers (`pushDuelState`/`pushDuelResult`/`pushChallengeExpired`, log-token expansion) are reused by ArenaService's sweeper. `arena:` callbacks also forwarded from Capital/MainController (invite answered after navigating away).
    │
    ├── Models/
    │   ├── User.swift              # Fluent model: identity, class, nickname, estate, profile style, game stats. **2026-05-18 currency rename**: `silver` Swift field + `@Field(key: "silver")` (was `gold`/`"gold"`; DB column renamed by `RenameGoldToSilver` migration). **2026-05-18 tutorial flag**: `tutorialTraderHintShown: Bool` (`@Field(key: "tutorial_trader_hint_shown")`, default false) — flipped to true on the first `ExplorationController.handleHomeReached`; `resetDevProfile` sets it true so dev iteration skips the hint. **Phase 6.4 fortune**: 3 fields — `activeFortuneCardId`, `activeFortuneExpiresAt`, `lastFortuneDrawAt` — plus `activeFortuneEffect` computed (returns the buff while in 6h window) and `fortuneSecondsRemaining` / `fortuneCooldownRemaining` helpers. Fortune additive bonuses fold into `effectiveAttack/Defense/Crit/Dodge/Accuracy`; `grantXP` multiplies by `xpMultiplier`. **Phase 5.3c**: `estateLevel` stored `@Field` (default 1). **Phase 5.3d**: `bagTier` stored `@Field` (default 1) — drives `InventoryEntry.slotCap(for:user)` via `BagCatalog.capForTier`. **Phase 5.3a XP/level**: `User.maxLevel = 21`, `User.xpRequiredToReach(_)` softcap helper (×2 to L5 then ×1.4), `xpToNextLevel` computed (Int.max at cap), `grantXP(_) -> XPGrantResult` loops level-ups + reports `levelsGained` / `estateLeveledUp` / new values. **Phase 5.3b stat growth**: `statGrowthLevels = {2,3,5,6,9,12,15,18}` + `statGrowthMaxHp = 5` / `statGrowthAttack = 1` / `statGrowthDefense = 1`. **2026-05-12 `isDeveloper`**: `var isDeveloper: Bool { developerUsers.contains(telegramId) }` extension — used by `InventoryEntry`/`WarehouseService`/`PlotService` to bypass per-unit slot caps for developer accounts. **Phase 7.1 guild membership**: `@OptionalParent guild` (`guild_id`, nullable FK → guilds, setNull on disband) + `guildRole` (`guild_role` string, raw `GuildRole` value) — one guild per player; roster is a `User` query by `guild_id`.
    │   ├── Item.swift              # Static item catalog: ItemType, ItemEffect, EquipmentSlot (8), GearStats, Item, ItemCatalog (code-based). Phase 5.2: Forester's leather set (hood / jerkin / breeches / boots) added; placeholder `gear.leather_vest` retired into `gear.forester_jerkin` via RenameLeatherVest. Phase 5.2.1: `Item.teachesRecipe: String?` field — when set on an artifact, the inventory action button switches from "✨ Use" to "📖 Learn"; 7 cooked dishes (food.baked_potato / roasted_meat / foragers_omelette / hunters_stew / meat_ragout / berry_tart / governors_feast) + 5 recipe scrolls (artifact.recipe.<dish_id>, non-stackable, no scrolls for the always-available starters baked_potato + roasted_meat) added.
    │   ├── Recipe.swift            # Static crafting catalog (RecipeCategory enum forge/tannery/kitchen, RecipeIngredient, RecipeOutput, Recipe, RecipeCatalog). Code-based like ItemCatalog/EnemyCatalog. RecipeCategory carries helpers (`requiresLearning` / `backCallbackData` / `actionButtonKey` / `craftedAlertKey`) so Workshop and Kitchen share the same renderer + handler while differing on action verb (Craft vs Cook), back target, and success banner ("Crafted ..." vs "Cooked ..."). `RecipeCatalog.starterRecipeIds: Set<String>` lists the always-available kitchen recipes (recipe.baked_potato, recipe.roasted_meat) — every player cooks these from day one without a LearnedRecipe row. Every kitchen recipe also burns 1× 🪵 mat.pine_lumber for the cooking fire.
    │   ├── LearnedRecipe.swift     # Phase 5.2.1 — Fluent model: per-user scroll-learned-recipe set (user_id FK cascade, recipe_id, learned_at). Helpers: `has(_:for:on:)`, `add(_:for:on:)` (idempotent — returns false on duplicate), `allIds(for:on:) -> Set<String>`. Stores only scroll-learned recipes; always-available starters are gated through `RecipeCatalog.starterRecipeIds` instead.
    │   ├── LearnedTechnique.swift  # Phase 5.3e — Fluent model: per-user known-technique set (user_id FK cascade, technique_id, learned_at; unique on user+id). Mirrors `LearnedRecipe` shape exactly. IDs are class-agnostic (`special_atk` / `special_def` / `super` — `CombatService.TechniqueKind.rawValue`); each player has at most 3 rows, one per kind, with the concrete technique resolved through `User.characterClass`. Helpers: `has` / `add` (idempotent) / `allIds`.
    │   ├── InventoryEntry.swift    # Fluent model: user_id, item_id, quantity, equipped_slot, tier (Phase 5.2.2; default 1, only consumed by tiered weapons in WeaponUpgradeCatalog), **Phase 6.5** durability/max_durability (init stamps 30; weapons get a per-tier ceiling 30→100, armor stays 30) + enchant_level (armor, cap 5) + add/remove/has/list/canAccept/slotsUsed helpers. **Phase 5.3d**: per-user `slotCap(for: User)` static func reading `BagCatalog.capForTier(user.bagTier)` — replaces the old `static let slotCap = 50`. T1=25 → T6=85 slots; equipped doesn't count. **2026-05-12 per-unit pivot**: `slotsUsed` sums `quantity` across non-equipped rows (was `count`). `canAccept` / `add` enforce `used + quantity <= cap` uniformly across stackable + non-stackable. Developer accounts (`user.isDeveloper`) bypass the cap check entirely — DB still records, UI shows overflow `X/Y` where X may exceed Y.
    │   ├── WarehouseEntry.swift    # Fluent model: estate storage, separate table; add/list/totalQuantity/remove helpers (remove added in Phase 5.2 to mirror InventoryEntry, used by CraftingService)
    │   ├── ExplorationState.swift  # Fluent model: one row per active or passive expedition (user_id unique, stepsDeep, visited_rooms JSON dict, mode, ends_at, report_json, running_report_json (per-step outcome+loot+hp/vigor snapshot for restart-safe passive reports) + the Phase 4 combat field stack). Presence = "exploring"; begin/beginPassive/current/end/allPassive helpers; recordVisit/visitCount + visitedRooms computed wrapper; isPassive/hasReadyReport/secondsRemaining queries; isInCombat / beginCombat (initialises 2 Special Atk + 2 Special Def + 1 Super uses + `combatRound = 0`) / endCombat (clears every combat-scoped column including combat_round); 4.2.1 stance helpers (hasActiveStance / beginStance / tickStance); 4.2.3 defense-effect helpers (hasEnemyDefDebuff / hasPlayerDodgeBuff / applyEnemyDefDebuff / applyPlayerDodgeBuff / tickDefenseEffects); per-fight budget helpers (hasSpecialAtkUse / hasSpecialDefUse / hasSuperUse / consumeSpecialAtk / consumeSpecialDef / consumeSuper). **2026-05-18 combat_round**: nullable `Int?` field bumped at the top of `CombatController.finishRound` so the first action surfaces "🌀 Раунд 1" in the status card; encounter intro stays clean (counter is 0). ExplorationMode enum (active / passive). The `returning` column is dormant from an earlier 3.2 design pass.
    │   ├── Enemy.swift             # Static bestiary (EnemyLootDrop + Enemy struct + EnemyCatalog). Code-based like ItemCatalog. 7 wilderness animals across 6 tiers + two special depthRange-0...0 mobs exploration never picks: `enemy.training_dummy` (HP 200, ATK 0, DEF 1, no loot — only spawned by the Training Ground plot) and `enemy.rabid_dog` (🐕 HP 18, ATK 14, DEF 1 — wild_boar level, no loot, xpReward 0; the one-off registration tutorial-fight mob, used by `RegistrationController.startDogFight`). **Phase 5.3a `xpReward: Int` field** on `Enemy` (T1=5 / T2=12 / T3=25 / T4=50 / T5=100 / T6=175 / training_dummy=0) — read by both `CombatController.finishVictory` (active) and `PassiveExpeditionService.recordOutcome` (passive autobattle). Wild family: 🐗 boar 1-10 / 🫎 moose 6-15 / 🦬 buffalo 11-20 / 🐻 wild_bear 21-30 (meat+hide). Rabid family: 🐈‍⬛ lynx 11-20 / 🐺 wolf 16-25 / 🐻‍❄️ rabid_bear 25-35 (hide only). Three deep-zone overlaps. Reference doc at `content/bestiary.md`.
    │   ├── Plot.swift              # Phase 5.1 — Fluent model: per-user estate plot. Fields: user_id, slot_index, plot_type, tier, last_harvested_at, notified_full. Production lazily computed from lastHarvestedAt + ratePerSecond × elapsed (capped). Helpers: list(for:), find(slot:for:), allUnfull(on:) (used by background ticker).
    │   ├── PlotCatalog.swift       # Phase 5.1 — code-based config: PlotType enum (farm / forest / mine / coop / trainingGround), PlotTuning (producedItemId, ratePerInterval, capacity, optional bonusOutput), PlotBonusOutput. Mine carries bonus output for mat.iron (1/interval, cap 20) alongside primary mat.river_pebble (8/interval, cap 40). `testMode` flag scales rates per-minute (test) vs. per-hour (prod). `tuning(for:)` returns nil for trainingGround so controllers route those to combat instead of harvest.
    │   ├── WeaponUpgradeCatalog.swift # Phase 5.2.2 — per-weapon tier ladder: WeaponUpgradeStep (stats + inputs) array indexed by tier-1, keyed by item id. Three weapons × 5 tiers. T1 stats match legacy Item.gearStats so existing equipped weapons see no numeric change after the migration. Helpers: step(for:tier:), stats(for:tier:), maxTier(for:), isUpgradable(_:). Sword crit ramps 0/3/6/10/15 per user spec; archer adds accuracy + crit; mage adds crit + accuracy. Materials stay within existing catalog (iron / iron_ingot / pine_lumber / river_pebble / clay / hide). Tiered weapons are blocked from warehouse deposit since WarehouseEntry has no tier column. **Phase 6.5 (2026-05-22)**: `durabilityByTier` [30,40,50,70,100] + `durability(forTier:)` — weapon durability ceiling per tier; upgrade refreshes the weapon to the new full max.
    │   ├── EstateUpgradeCatalog.swift # Phase 5.3c — estate-tier progression. `EstateUpgradeStep` (toTier, requiredPlayerLevel, inputs, **goldCost**) × 6 transitions T1→T2 ... T6→T7. Player-level gates 4/7/10/13/16/19. Materials scale: ~33 units (T1→T2 onramp, lumber+pebble+hide) to ~313 units (T6→T7 endgame, all 6 materials). Gold gate kicks in at T3+ (50g → 150g → 400g → 1000g). Helpers: `nextStep(from:)`, `canUpgrade(from:)`, `maxTier = 7` constant. Pure data, no DB writes — `EstateUpgradeService` consumes it.
    │   ├── TraderCatalog.swift      # Phase 6.1 + v2 economy rebase (2026-05-17): static catalog of capital-trader listings. `TraderListing` carries item id + asymmetric packets `(sellPacketQty, sellPacketGold)` / `(buyPacketQty, buyPacketGold)`. All packets = 1 post-rebase (per-unit pricing). 11 listings split into 5 tiers + crafted: T1 (pebble/berries/nuts) 1g/2g · T2 (lumber/clay) 2g/4g · T3 (potato/egg/hide) 3g/6g · T4 (raw_meat) 5g/10g · T5 (iron) 10g/20g · crafted (iron_ingot) 100g/200g. Flat 2× sell:buy spread. Iron sits at 10× pebble per-unit value to reward the 1/5 foraging weight; ingot at 10× iron matches the 10-lump smelting recipe (sell-neutral between selling raw vs ingot, but ingot saves 9 bag slots).
    │   ├── FortuneCatalog.swift     # Phase 6.4 (2026-05-17): static catalog of 22 Major Arcana tarot cards for the Ворожка at the capital. `FortuneEffect` struct carries 14 optional fields (additive stat bonuses for attack/defense/crit/dodge/accuracy, multipliers for xp/loot/vigor-drain, one-shot deltas for silver/xp/HP/vigor restore, Wheel-style random silver positive/negative — fields named `oneShotSilver` / `randomSilverPositive` / `randomSilverNegative` since the 2026-05-18 currency rename) — each card sets only the slice it cares about. `FortuneCard` (id / nameKey / meaningKey / buffDescKey / effect) auto-derives the 3 locale-key paths from `id`. Catalog constants: `drawPrice = 10` silver, `buffDurationSeconds = 6h`, `cooldownSeconds = 24h` (split into two separate windows). 22 cards covering 0_fool through 21_world; effect mix 7/3/7/2/2/1 (buff/debuff/mixed-24h/positive-one-shot/mixed-one-shot/negative-one-shot — user-requested trickster lean). `assetPath(for:)` returns `Assets/capital/fortune/<id>.png`.
    │   ├── TavernCatalog.swift      # Phase 6.2: static catalog of tavern offerings. `TavernFoodListing` carries item id + `priceGold` (single integer per dish, no spread — tavern only sells food, doesn't buy). 7 cooked dishes: baked_potato 20g · roasted_meat 30g · foragers_omelette 50g · berry_tart 60g · meat_ragout 80g · hunters_stew 100g · governors_feast 200g. Pricing ~2.5× DIY material cost via trader buy prices, lands between "5× old prices" and "DIY×markup" whichever was rounder. **All 7 dishes sold regardless of recipe knowledge** — bypassing scroll learning is the tavern's value prop. `wagerTiers = [10, 25, 50]` (shared between dice + darts).
    │   ├── TravelState.swift        # Phase 6.0: Fluent model — one row per in-flight trip between estate and capital. Fields: user_id (unique FK cascade), destination ("capital" | "estate"), ends_at (Date). `TravelDestination` enum, `destination` computed wrapper, `secondsRemaining(now:)` countdown helper, static `current(for:on:)` / `begin(...)` / `end(...)` / `allInflight(on:)` helpers (same shape as `ExplorationState`). Presence of a row = en route; flipped to `User.location` when the timer elapses, then deleted.
    │   ├── TavernGameMessage.swift  # 2026-05-20: Fluent model for the 24h tavern-cleanup. One row per chat message a dice/darts round leaves (label/die/result). Fields: telegram_id (Int64, raw chat id — no User FK, sweep is a flat query), message_id (Int), created_at. `TavernCleanupService` deletes rows + their TG messages once created_at ages past 24h (Telegram blocks deleting private-chat dice younger than that).
    │   ├── MasterCatalog.swift     # Phase 6.5: the Master's shop economics (pure data). `armorForSale` (Forester set premium buy prices, set 485🪙 ≈4× material value), `repairCost(itemId:missing:)` (armor, ≈half buy price for a full repair), `weaponRepairCost(missing:)` (1🪙/point), `enchantSteps` (40/100/220/450/850🪙 + hide) + `enchantCap` (5) + `enchantBonusPoints(level:)` non-linear curve (per-level weights 1/1/1/2/3 → cumulative 1/2/3/5/8). Durability runtime + weapon tier ceilings live in GearConditionService / WeaponUpgradeCatalog.
    │   ├── MarketListing.swift     # Phase 6.5 (2026-05-28): Fluent model — one row per live player-to-player lot (seller_id FK cascade, item_id, quantity, price [whole-lot silver], created_at). `unitPrice` computed (price/quantity, display+sort only). Read helpers: allActive / forSeller / forItem (cheapest-per-unit first) / find(id:) / activeCount / itemSummaries(excludingSeller:) → [(itemId, lotCount, minUnitPrice)] for the Level-1 item-grouped buy board. Escrow lives on the row (units already removed from the seller's bag); `MarketService` owns the transactions.
    │   ├── MarketCatalog.swift      # Phase 6.5: pure tuning constants for the Market — `listingFee = 5` (flat silver sink, non-refundable, charged at lot creation) + `maxActiveLots = 5` (per-seller cap). Tunable one-liners (like TavernCatalog).
    │   ├── Guild.swift              # Phase 7.1: Fluent model — `guilds` (name unique, tag [admin-assigned, often empty], emblem, leader_id Parent, treasury Int silver, motto?). Read helpers: find(id:) / named(_:ILIKE) / all / members(on:) [User query by guild_id] / memberCount / officerCount.
    │   ├── GuildCatalog.swift       # Phase 7.1: tuning — memberCap 20, maxOfficers 2, foundCost 500🪙, foundLevelGate 5, defaultEmblem 🛡, nameMin/Max 3/24, vaultUnitCap 3000. `GuildRole` enum (leader/officer/member) + `canManageMembers`/`canWithdrawVault` (both = leader||officer). Pure data.
    │   ├── ArenaProfile.swift        # Phase 8.3 (2026-07-20): Fluent model — `arena_profiles` (user_id unique Parent, honor Int, wins, losses, fights_today, fights_day_stamp yyyy-MM-dd UTC). Helpers: forUser (lazy-create), fightsSpentToday/bumpDailyCounter (day rollover), leaderboard(limit:), dayStamp(_).
    │   ├── ArenaCatalog.swift        # Phase 8.3: tuning — stakeTiers [25/100/500], tithePercent 10 (King's silver sink), startingHonor 1000, honorKFactor 32, turnSeconds 45, maxMissedTurns 2, challengeTTL 120, lobbyTTL 180, dailyFightCap 20, leagueKey(forHonor:) → Новак/Боєць/Ветеран/Чемпіон. Pure data.
    │   ├── QuestCatalog.swift        # Phase 9.2 (2026-08-23): daily-quest content + assignment. `QuestNPC` (trader/master/tavern; boardTitleKey + backCallback), `QuestCounter` (beastKill / ironIngotForged / gambleWin / traderSilver), `QuestObjective` (.deliver(itemIds,count) / .counter(counter,target)), `QuestReward` (silver/xp/vigor), `QuestDef`. `pools` = 3 jobs per NPC. `daily(npc:userId:stamp:)` picks via FNV-1a `stableHash("userId:npc:gameDay")` % poolCount — deterministic, restart-proof, never stored (Swift's `Hasher` is per-process seeded, hence the hand-rolled FNV). `find(id)` re-hydrates a stored row.
    │   ├── QuestProgress.swift       # Phase 9.2 (2026-08-23): Fluent model — `quest_progress` (user Parent, npc, quest_id, day_stamp, progress, claimed, timestamps). One row per (player, NPC, game day); `quest_id` copied at creation so a mid-day catalog edit can't swap a started job. Rows are never deleted (yesterday's simply stops matching today's stamp — free history for future streaks). Helper: `find(userId:npc:stamp:on:)`.
    │   ├── GuildInvite.swift        # Phase 7.1: Fluent model — `guild_invites` (guild/invitee/inviter Parents, created_at). Helpers find/forInvitee/exists/clearAll. Deleted on accept/decline.
    │   ├── GuildVaultEntry.swift    # Phase 7.1: Fluent model — `guild_vault` (guild_id Parent, item_id, quantity). Mirrors WarehouseEntry but keyed by guild; stackables only. add/list/totalQuantity/totalUnits/remove. Withdraw permission enforced in GuildService, not schema.
    │   ├── BagCatalog.swift         # Phase 5.3d (+ 2026-05-12 expansion) — bag-tier ladder. `BagUpgradeStep` (toTier, capacity, requiredEstateLevel, inputs) × 5 transitions T1→T2 ... T5→T6. Capacities `[25, 35, 45, 60, 80, 85]` indexed by tier-1 (was `[20, 30, 40, 55, 75]` before the per-unit pivot — bumped +5 across the chain and added a T6 capstone). Estate-tier gates per step: T2 reqs estate T3, T3 reqs T4, T4 reqs T5, T5 reqs T6, **T6 reqs T7** (Lord's Holdings — endgame craft). Materials hide + iron / iron_ingot only — no silver cost on the bag track. Helpers: `capForTier(_)`, `nextStep(from:)`, `canUpgrade(from:)`, `maxTier = 6`. Consumed by both `InventoryEntry.slotCap(for:)` and `BagUpgradeService`.
    │
    ├── Migrations/
    │   ├── CreateUser.swift        # users table: id, telegram_id, router_name, locale, names
    │   ├── AddCharacterFields.swift # nickname, character_class, estate_name, registration_step
    │   ├── AddProfileStyle.swift   # profile_style (1-3)
    │   ├── AddGameStats.swift      # level, xp, hp, max_hp, vigor, max_vigor, atk/def/crit/dodge/acc, gold (column originally `gold`; renamed to `silver` by `RenameGoldToSilver` on 2026-05-18 — this historical file kept untouched per never-edit-migration-history rule), crowns (later dropped via `RemoveCrownsField`)
    │   ├── CreateInventory.swift   # inventory table: user_id (FK, cascade), item_id, quantity, timestamps
    │   ├── RemoveCrownsField.swift # drops crowns column; premium currency name TBD
    │   ├── AddEquipSlotToInventory.swift # nullable equipped_slot column on inventory (Phase 2.3.2)
    │   ├── AddGearBonuses.swift    # 5 cached gear_*_bonus fields on users (Phase 2.3.2)
    │   ├── CreateWarehouse.swift   # warehouse table (Phase 5 — estate storage, separate from inventory)
    │   ├── CreateExplorationState.swift # exploration_state table (Phase 3.1): per-user stepsDeep, unique on user_id, cascades on user delete
    │   ├── AddExplorationReturnState.swift # (Phase 3.2): adds `visited_rooms` TEXT (JSON dict km → visit count) and a dormant `returning` Bool to exploration_state
    │   ├── AddHpRegenTick.swift    # (Phase 3.2 polish): adds `last_hp_tick_at` nullable Date to users
    │   ├── AddPassiveExpeditionFields.swift # (Phase 3.3): adds nullable `mode`, `ends_at`, `report_json` to exploration_state
    │   ├── RenameMaterialIds.swift # Data migration: mat.wood→mat.pine_lumber, mat.stone→mat.river_pebble, mat.iron_ore→mat.old_iron (inventory + warehouse)
    │   ├── RenameFoodIds.swift    # Data migration: food.berry→food.forest_berries; bread/stew/roast rows deleted outright
    │   ├── AddCombatFields.swift  # (Phase 4.1): adds nullable `combat_enemy_id` (string) + `combat_enemy_hp` (int) to exploration_state — embeds live combat in the expedition row
    │   ├── AddCombatStanceFields.swift  # (Phase 4.2.1): adds nullable `combat_stance` + `combat_stance_rounds_left` for Super-technique stances
    │   ├── AddCombatDefenseFields.swift # (Phase 4.2.3): adds nullable `combat_enemy_def_debuff` + `combat_player_dodge_buff` rounds-remaining counters for Iron Bulwark / Shadow Veil persistent effects
    │   ├── AddCombatTechniqueUses.swift # (Phase 4.2 polish): adds per-fight budget counters `combat_special_atk_uses` (max 2), `combat_special_def_uses` (max 2), `combat_super_uses` (max 1)
    │   ├── CreatePlots.swift       # (Phase 5.1): creates `plots` table — user_id (FK cascade), slot_index, plot_type, tier, last_harvested_at, notified_full. Unique on (user_id, slot_index).
    │   ├── RemoveOldIron.swift     # (Phase 5.1): data-only DELETE FROM inventory/warehouse WHERE item_id = 'mat.old_iron'. Retires the legacy item; its role (medium-tier iron-from-foraging) is now `mat.iron` (Iron Lump 🔩).
    │   ├── RenameLeatherVest.swift # (Phase 5.2): data-only UPDATE inventory/warehouse SET item_id = 'gear.forester_jerkin' WHERE item_id = 'gear.leather_vest'. Pure rename — existing rows (worn, in bag, in warehouse) carry over with the new +3 DEF stats.
    │   ├── CreateLearnedRecipes.swift # (Phase 5.2.1): creates `learned_recipes` table — user_id (FK cascade), recipe_id, learned_at. Unique on (user_id, recipe_id). Stores the per-user known-recipe set used to gate Kitchen recipes.
    │   ├── AddPassiveRunningReport.swift # (Phase 3.3 polish): adds nullable `running_report_json` TEXT column to `exploration_state`. Persists the in-flight outcome counters + loot totals + hp/vigor-at-start snapshot so a bot restart resumes the passive simulation with a complete event history (the in-memory locals in runLive used to reset on restart, leaving the final report incomplete).
    │   ├── AddInventoryTier.swift   # (Phase 5.2.2): adds `tier INT NOT NULL DEFAULT 1` to `inventory`. Default 1 means existing equipped weapons stay at the legacy stats (T1 in WeaponUpgradeCatalog matches Item.gearStats); only weapons in the catalog actually read the column.
    │   ├── AddEstateLevel.swift     # (Phase 5.3c): adds `estate_level INT NOT NULL DEFAULT 1` to `users`. Converts the previously-computed `User.estateLevel` to a stored field — existing players land at T1 (matches the old derivation at L1) and have to spend materials via `EstateUpgradeService` to advance.
    │   ├── AddUserBagTier.swift     # (Phase 5.3d): adds `bag_tier INT NOT NULL DEFAULT 1` to `users`. Existing players land at T1 (20 slots — down from the previous flat 50); rows already in their bag stay readable, only new inserts are blocked past the cap. Players sew bigger bags at the Workshop via `BagUpgradeService`.
    │   ├── CreateLearnedTechniques.swift # (Phase 5.3e): creates `learned_techniques` table — user_id (FK cascade), technique_id, learned_at. Unique on (user_id, technique_id). Stores the per-user known-technique set used to gate the combat submenu's three kinds (special_atk / special_def / super).
    │   ├── AddUserLocation.swift    # (Phase 6.0): adds `location VARCHAR NOT NULL DEFAULT 'estate'` to `users`. Stores where the player physically is — "estate" or "capital". Flipped only by `TravelService` on trip arrival, never by a controller. Existing players land at "estate" (the default value matches their narrative position pre-Phase-6).
    │   ├── CreateTavernGameMessages.swift # 2026-05-20: creates `tavern_game_messages` table (telegram_id Int64, message_id Int, created_at). No FK — flat sweep query. Backs the 24h tavern dice cleanup.
    │   ├── CreateTravelState.swift  # (Phase 6.0): creates `travel_state` table — user_id (FK cascade), destination (string, "capital"|"estate"), ends_at (datetime, required), created_at/updated_at. Unique on user_id (one trip per player). Cascade-deletes with the user. Presence of a row = en route; absence = stationary at `user.location`.
    │   ├── AddFortuneFields.swift   # (Phase 6.4): adds nullable `active_fortune_card_id` (string, catalog key like "19_sun") + `active_fortune_expires_at` (datetime, when the 6h buff wears off) to users. Both nil until first draw.
    │   ├── AddFortuneCooldownField.swift # (Phase 6.4 split-fix): adds nullable `last_fortune_draw_at` (datetime) to users. Split out from the buff expiry above so the 24h draw cooldown and the 6h buff window can have different durations. Stamped to "now" on every successful draw.
    │   ├── RenameGoldToSilver.swift # (2026-05-18 currency rename): raw-SQL `ALTER TABLE users RENAME COLUMN gold TO silver`. Renames the currency column to match the new in-game name (gold→срібники/silvers). Existing balances survive. Reverse migration flips it back.
    │   ├── AddTutorialTraderHint.swift # (2026-05-18 polish pass): adds `tutorial_trader_hint_shown` (bool, default false) to users. Flipped to true on the first clean `ExplorationController.handleHomeReached` so the one-shot "there's a Trader in the Capital" tip fires exactly once per player.
    │   ├── AddCombatRound.swift # (2026-05-18): adds nullable `combat_round` (Int) to `exploration_state`. `beginCombat` sets to 0; `finishRound` bumps before render so the first player action surfaces "Раунд 1" in the status card; `endCombat` clears.
    │   ├── AddGender.swift # (2026-05-21): adds nullable `gender` (string, "m"/"f") to users. Chosen at registration step 1 (ahead of name); drives uk feminitives (`Lingo.localize(_:gender:locale:)`) + per-gender estate art. Nil-safe (treated as male); dev-reset clears it so test profiles re-pick.
    │   ├── CreateMarketListings.swift # (Phase 6.5, 2026-05-28): creates `market_listings` table — seller_id (FK users cascade), item_id, quantity, price (int, whole-lot silver), created_at/updated_at. No unique constraint (a seller may hold several lots; `maxActiveLots` is enforced in the service, not the schema). Backs the player-to-player Market.
    │   ├── CreateGuilds.swift        # Phase 7.1: `guilds` (name unique, tag, emblem, leader_id FK users cascade, treasury, motto, timestamps). Runs BEFORE AddUserGuildFields (FK target).
    │   ├── CreateArenaProfiles.swift  # Phase 8.3 (2026-07-20): `arena_profiles` (user_id unique FK users cascade, honor, wins, losses, fights_today, fights_day_stamp, timestamps).
    │   ├── CreateQuestProgress.swift  # Phase 9.2 (2026-08-23): `quest_progress` (user_id FK users cascade, npc, quest_id, day_stamp, progress, claimed, timestamps). Unique on (user_id, npc, day_stamp) — "one job per NPC per day" enforced by the DB, not by convention.
    │   ├── AddUserGuildFields.swift  # Phase 7.1: adds `guild_id` (nullable FK guilds, onDelete setNull) + `guild_role` (string) to users.
    │   ├── CreateGuildInvites.swift  # Phase 7.1: `guild_invites` (guild_id/invitee_id/inviter_id FKs cascade, created_at).
    │   ├── CreateGuildVault.swift    # Phase 7.1: `guild_vault` (guild_id FK cascade, item_id, quantity, timestamps). Shared stackable storage.
    │   └── AddGearCondition.swift # (2026-05-21): Phase 6.5 — adds `durability` + `max_durability` (default 30) + `enchant_level` (default 0) to inventory. Backs the Master's repair/enchant + per-fight wear. 2026-05-22 expansion reuses the same columns for weapons (per-tier max, half-stats at 0) — no schema change.
    │
    ├── Services/
    │   ├── VigorService.swift    # Pure: VigorAction enum (walkRoom / walkRoomDoubleSpeed / combatRound (passive) / combatAttack/Defend/Flee (active, costs 2/1/3) / idle), drain (with optional multiplier for stance buffs like Bloodlust ×2), consume, isStarving, applyStarvationHPLoss, starvation penalty on effective ATK/DEF (on User via extension); effective-stat extension also includes crit/dodge/accuracy + gear bonuses
    │   ├── EquipmentService.swift # equip (atomic slot swap), unequip, equipped(for:), recomputeBonuses (writes cached gear_*_bonus on User by summing `contributedStats` over equipped rows). **Phase 6.5 (2026-05-22)**: `nominalStats(of:for:)` = full-condition stats incl. armor enchant (flat +DEF + class stat ⚔️ DEF / 🏹 dodge / 🔮 crit, scaled by `MasterCatalog.enchantBonusPoints`); `contributedStats(of:for:)` applies durability — armor at 0 broken (→0), weapon at 0 keeps half. Single source shared with the inventory detail card.
    │   ├── WarehouseService.swift # deposit / withdraw — moves one unit between InventoryEntry and WarehouseEntry; returns typed enum (success / nothingToTransfer / notTransferable / **warehouseFull (Phase 5.3c)** / inventoryFull on withdraw); deposits skip equipped rows; withdraw preflights backpack space. **2026-05-12 per-unit pivot**: `capForLevel(_)` table scaled ×4 to T1=200 → T7=2000 (was T1=50 → T7=500); `slotsUsed(for:on:)` sums `quantity` across rows (was distinct-row count); deposit's `needsNewRow` shortcut removed — every unit eats one slot. `depositAll` now partial-fills (10-unit hide row with 6 free slots dumps 6, leaves 4 behind) instead of skipping. Developer accounts (`user.isDeveloper`) bypass the cap entirely on both single and depositAll paths. **2026-05-15 `withdrawN`**: bulk-withdraw companion to `withdraw`. Returns `WithdrawNResult` (`.success` / `.notEnoughInWarehouse(available:)` / `.inventoryFull(free:)`) — both failure cases carry the limiting number so the UI surfaces precise messaging without a follow-up query. Atomic preflight (source has ≥ quantity AND destination free ≥ quantity), then `WarehouseEntry.remove` + `InventoryEntry.add`. Powers the warehouse `[✏️ N]` button in EstateController. **2026-05-15 part 2 `depositN`**: mirror of `withdrawN` flipped — `DepositNResult` (`.success` / `.notEnoughInBag(available:)` / `.warehouseFull(free:)` / `.notTransferable`). Source = bag (counts unequipped rows only), destination = warehouse (cap-checked per-tier, dev bypass via `user.isDeveloper`). Tiered weapons return `.notTransferable` defensively — the `[✏️ N]` button only renders on stackable items so this shouldn't fire in practice.
    │   ├── ExplorationService.swift # Phase 3.1/3.2 (re-tuned 2026-05-12): rollStep(mode:) with `priorVisits:Int` three-tier weight table — fresh `nothing 10 / loot 50 / encounter 30 / trip 10` → reduced `nothing 20 / loot 50 / encounter 20 / trip 10` → bare `nothing 80 / loot 20 / encounter 0 / trip 0`. (Pre-2026-05-12: 20/40/30/10 → 50/20/20/10 → 100/0/0/0; "nothing" felt too quiet, bare tier was unwinnable). Active mode returns `.encounterStarted(enemy)` so CombatController takes over; passive mode runs `resolveAutobattle` on top of `CombatService.applyAttack` and returns `.encounterWon` / `.encounterLost`. `awardEncounterDrops` is a public hook for CombatController's victory path (mirrors the autobattle's drop step). StepOutcome enum: nothing / loot / trip / encounterStarted (active hand-off) / encounterWon / encounterLost / starvationOnly.
    │   ├── HealingService.swift # Phase 3.2 polish: lazy-compute passive HP regen (5%·maxHp per minute) while player is NOT on any expedition (active or passive) and hp < maxHp. `tick(_:inExpedition:on:)` is called from RouterStore.process on every interaction — RouterStore queries ExplorationState presence to derive `inExpedition`. Pins `user.lastHpTickAt` at full HP and clears it during expedition to prevent banked regen.
    │   ├── PassiveExpeditionService.swift # Phase 3.3: passive expedition. PassiveDuration (30/60/90 units), testMode flag (seconds vs minutes), start() arms a Task.detached running runLive (live per-step loop with Task.sleep between each step + absolute-fire-time catch-up after restart). Early exit on death pushes the report immediately. Progress tracked via state.stepsDeep so restart resumes at the right step. **`RunningPassiveReport` Codable snapshot** (hpBefore, vigorBefore, outcomeCounts, lootPicked, lootDropped, **xpEarned (Phase 5.3a)**) is persisted on `state.running_report_json` after every step in the same save as `stepsDeep` — restored on the resumed runLive's first iteration so passive reports stay complete across restarts. `start()` snapshots HP/vigor at expedition begin. **Phase 5.3a XP integration**: `recordOutcome` accumulates `enemy.xpReward` on every `.encounterWon`; `finalizeAndPush` calls `user.grantXP(xpEarned)` and `saveAndCache` before serializing the report so death's inventory wipe doesn't touch the level-up. `PassiveReport` (final blob) gains `xpEarned` / `levelsGained` / `newLevel` plus **Phase 5.3b** `maxHpGained` / `attackGained` / `defenseGained` fields with a custom `init(from: any Decoder)` that defaults missing keys to 0 — backwards-compat for in-flight pre-5.3a/5.3b rows. `renderReport`'s XP line is built from 3 composable fragments — base XP (`exploration.passive.report.xp`), level-up segment (`.levelup`), stat-boost segment (shared `level_up.stat_boost`). Final `PassiveReport` Codable blob stored in exploration_state.report_json. rescheduleInflight() runs on bot startup via configure.swift.
    │   ├── CombatService.swift # Phase 4.1 + 4.2 + 4.3.1 + 5.1 + 5.3e: shared damage primitives plus all class-technique tunings. `applyAttack(...)` with optional `AttackModifiers` (hitChanceModifier / defenderDEFFraction / critBonus / cannotMiss / flatDamageBonus); `chipDamage(attackerATK:defenderDEF:extraMultiplier:)` for Defend's parry-counter (default 30% of base; archer passes 0.5 multiplier for 15%). **2026-05-15** `Defend` namespace: `archerChipMultiplier = 0.5`, `archerDodgeBonus = 30` (flat dodge for the round), `mageBarrierDamageFraction = 0.4` (60% damage reduction on landed enemy hit). Powers the per-class basic Defend branch in `CombatController.onDefend`. 4.2.1 stance layer: `StanceModifiers` struct, `stanceModifiers(for:)` lookup (Bloodlust / Hawk's Eye / Arcane Resonance), `stanceId(forClass:)`, `stanceActivationVigor(for:)`, `stanceDurationRounds = 3`. 4.2.2 special-attack layer: per-class `specialAttackVigor(forClass:)`, `specialAttackModifiers(forClass:)`, `specialAttackZeroesDodge(forClass:)`. 4.2.3 special-defense layer: `SpecialDefense` namespace + chip / dodge / reflect tunings. 4.3.1 Flee tuning: `Flee` namespace (warrior 40 / archer 70 / mage 90 success %, mage `mageVigorExtra = 2`). 5.1 Training Ground identity: `trainingDummyEnemyId` constant — checked by CombatController to flip into training mode (clean damage, no vigor drain, no enemy counter, dummy auto-revives). **Phase 5.3e**: `TechniqueKind` enum (specialAtk / specialDef / super, class-agnostic IDs) + `requiredLevel(for:)` (L8 / L11 / L14 thresholds) + `initialUses(for:playerLevel:)` (1 → 2 at L17 / L20 / L21) + `initialUsesForUser(_)` helper used by every `beginCombat` caller.
    │   ├── PlotService.swift   # Phase 5.1: pure plot helpers. `accumulated(for:)` / `bonusAccumulated(for:)` lazy-compute primary + bonus yield from lastHarvestedAt + ratePerSecond × elapsed (capped). `harvest(_:for:on:)` returns `HarvestResult.success(primary:bonus:)` and deposits into `WarehouseEntry`. `claim(slot:type:for:on:)` validates slot allowance via `slotsForLevel(_:)`. **Phase 5.3c**: slot table restored from flat 5 to `[0, 1, 2, 3, 4, 5, 6]` indexed by tier-1 — T1 has zero slots, first slot opens at T2. `hasAnyPlot` helper deleted (only caller was registration auto-Farm grant, now also gone — T1 starts with empty land per the wooden-hut narrative).
    │   ├── GearConditionService.swift # Phase 6.5: gear durability runtime. `maxDurabilityStart` 30, `repairMaxShave` 1 (armor only), `armorSlots` / `weaponSlots` (main_hand) / `durableSlots` (union). `WearEvent` victory 1 / defeat 3 / flee 5. `drainEquippedGear(amount:for:on:)` (renamed from drainEquippedArmor) spends a fight's wear *budget* point-by-point across the shared armor+weapon pool (model C), then recomputes bonuses. `wear(_:for:on:)` convenience. `backfillWeaponDurability(on:)` — idempotent startup pass lifting pre-existing weapons (flat 30) to their per-tier ceiling. At-0 behaviour (armor broken / weapon half) lives in EquipmentService.
    │   ├── MarketService.swift     # Phase 6.5 (2026-05-28): pure DB-only orchestrator for the player-to-player Market (no bot I/O — controller renders + pushes the seller notification). `createListing(itemId:quantity:price:for:on:)` → CreateResult (success / notSellable / tooManyLots(max) / notEnoughInBag(have,need) / notEnoughSilver(have,fee)); validates stackable + lot cap + bag + fee, then debits the flat listing fee, escrows the units off the bag (`InventoryEntry.remove`), writes the lot. `buyListing(id:buyer:on:)` → BuyResult (success(itemId,qty,price,sellerTelegramId,sellerLocale,sellerGender) / notFound / ownListing / notEnoughSilver / inventoryFull(free,need)); re-reads the lot so a raced double-buy surfaces as notFound, then debits buyer + credits seller (loads seller User, saveAndCache) + delivers items + deletes lot. `cancelListing(id:seller:on:)` → CancelResult (success / notFound / notOwner / inventoryFull(free,need)); returns escrowed units to the seller, deletes lot, **fee not refunded**. Mirrors TraderService/MasterService style.
    │   ├── TradeStore.swift        # Phase 6.5 (2026-06-10): in-memory `actor` for the synchronous player-to-player trade (Market → 🤝 Обмін). Holds `lobby` (presence room, telegramId→lastSeen+nickname, 180s TTL), `sessions` (keyed by UUID = source of truth), `byUser` (busy-index, BOTH participants' telegramIds → session UUID). Value types: `Side` (offeredStacks/offeredGear/silver/firstConfirmed/secondConfirmed/screenMessageId + locale+nickname copied in for sweeper), `TradeSession` (a/b/phase/lastActivity), `Phase` (pendingAccept/building/locked/committing). Methods return decision-snapshots (Telegram I/O stays in the controller): touchLobby / lobbyMembers(excluding:) [fresh+non-busy] / invite → InviteResult / accept → AcceptResult / decline / toggleStack/setStack/toggleGear/setSilver (mutateBuilding resets ONLY the editor's ready-flag since 2026-06-15 — the partner's stage-1 «Погодити» survives your edits so they don't re-tap; the locked-summary stage still re-confirms both, so committing a changed deal stays impossible) / confirmFirst/confirmSecond → ConfirmResult(.waiting/.both/.noop) / beginCommit (CAS into .committing) / cancel/finish/teardown / sweep. `startSweeper(bot:lingo:)` detached loop (mirrors TavernCleanupService) drops stale presence + cancels idle trades, pushing both sides a localized timeout notice. NOT persisted — restart cancels in-flight trades.
    │   ├── TradeService.swift      # Phase 6.5 (2026-06-10): DB side of the trade. `tradeableBagItems(for:on:)` → (stacks:[(itemId,qty)], gear:[InventoryEntry]) = non-equipped, non-bound (`WeaponUpgradeCatalog.isUpgradable` excluded) bag rows; stackables summed, gear as individual rows. `commit(session:on:)` → CommitResult(.success/.failed(Reason: itemGone/noSilver/bagFull, each w/ nickname)). No Fluent transaction exists, so VALIDATE-EVERYTHING-FIRST (stacks present, gear rows still owned+unequipped+unbound, silver, capacity formula `used − out + in ≤ cap`) then MUTATE in order: remove both sides' stacks → reassign gear rows (`$user.id` swap, preserves enchant/durability/tier, no cap check) → add stacks to the other side → swap silver + saveAndCache. Order guarantees the stack-add cap check exactly equals the proven inequality, so no mid-swap throw.
    │   ├── GuildService.swift       # Phase 7.1: DB side of guilds (no bot I/O). `found` (level+silver+name-unique checks → burn 500🪙, create Guild with EMPTY tag, stamp leader membership), `leave` (non-leader only), `disband` (clear all members + delete guild, cascades invites+vault), `invite(targetIdentifier:by:)` (findTarget = @username then nickname ILIKE; perm+self+already-in+full+dup checks → write GuildInvite, returns invitee tg/locale for push), `acceptInvite`/`declineInvite`, `kick` (leader>officer>member rank rules), `setOfficer(makeOfficer:)` (leader only, cap 2). Vault: `depositToVault`/`withdrawFromVault` (stackables; deposit any member, withdraw leader+officers; cap 3000). Treasury: `depositSilver`/`withdrawSilver` (User.silver ↔ Guild.treasury; same perms). Typed result enums throughout; validate-then-mutate.
    │   ├── ArenaStore.swift         # Phase 8.3 (2026-07-20): in-memory `actor` for the live PvP duel (герць), TradeStore-shaped. Holds `lobby` (presence, 180s TTL), `pending` (issued-but-unanswered challenges by UUID), `duels` (live fights by UUID), `byUser` (busy-index → pending OR duel UUID). Value types: `Combatant` (frozen atk/def/crit/dodge/acc + live hp + stake + pre-duel honor + defending/missedTurns), `PendingChallenge`, `Duel` (a/b/turn/round/turnDeadline/phase). Methods return decision-snapshots (Telegram I/O in controller): touchLobby/lobbyMembers/challenge→ChallengeResult/cancelPending(Involving)/startDuel/`resolve(action:tg:)`→ActionResult(.continued/.ended/.notYourTurn/.noDuel) [combat dice rolled INSIDE the actor via CombatService.applyAttack so roll+HP mutation are atomic]/surrender/sweep→SweepOutput(expiredChallenges/timedOutTurns/forfeits). NOT persisted — restart cancels in-flight duels (no settlement fires → zero financial effect since silver only moves at settle).
    │   ├── ArenaService.swift       # Phase 8.3: DB side of the arena (no live state). `snapshot(user,stake,honor)` (effective stats + current hp), `validateMatch` (both alive + solvent + under daily cap; NO silver moved), `honorAfter(winner,loser)` (symmetric ELO, K=32), `settle(Ended)` (transfer stake loser→winner minus King's tithe = burned silver sink, current-HP carry-over floored at 1 non-lethal, W-L tally, daily counter, ELO write) → Settlement. `startSweeper(db,bot,lingo)` detached loop: challenge expiry + turn timeouts (auto-defend) + forfeit settlement, pushing via ArenaController static render helpers.
    │   ├── QuestService.swift      # Phase 9.2 (2026-08-23): the daily-quest loop. `activeQuest` / `status` (board state in one read — deliver progress counted LIVE from the bag, counter progress from the row; lazily creates the day's row), `record(counter:amount:for:on:)` (ticked by 5 hook sites, derives each NPC's job before touching the DB so non-matching events cost nothing), `finish(npc:)` (deliver → drain items in catalog order then pay; counter → require target then pay), private `payOut` (silver + `grantXP` + Vigor clamped to cap → `saveAndCache`; returns the XPGrantResult so banners can echo level-up lines).
    │   ├── MasterService.swift     # Phase 6.5: the Master's three actions, all draining User.silver. `buy(itemId:)` (silver + slot check → add at full durability), `repair(entryId:)` — branches armor (restore to max−1, ½ buy price, mechanic B) vs weapon (restore to full, 1🪙/point, NO shave), `enchant(entryId:)` (armor, flat +DEF + class stat, drains silver + hide). Typed result enums; mirrors TraderService/WeaponUpgradeService. isArmor / isWeapon by slot.
    │   ├── TavernCleanupService.swift # 2026-05-20: deletes tavern dice/darts clutter on a 24h delay (Telegram forbids deleting a private-chat dice message until it's >24h old). `record(messageIds:telegramId:on:)` persists a round's message ids to `tavern_game_messages`; `startSweeper(on:bot:)` (started in configure.swift, mirrors PlotProductionService) runs a Task.detached loop — catch-up sweep on boot, then every 30 min — deleting each message + row once created_at passes `deletableAfter` (24h + 60s margin). Dice stay in chat as game history until then.
    │   ├── PlotProductionService.swift # Phase 5.1: single Task.detached ticker started from configure.swift after bot.start. `tickInterval` is 60s in test mode / 300s in prod. Each tick walks `Plot.allUnfull(on:)` (plots with notified_full = false), checks if accumulator hit cap, pushes a "🌾 ready to harvest" message to the owner's chat, flips `notified_full = true` to suppress repeats. Harvest resets the flag.
    │   ├── CraftingService.swift # Phase 5.2: pure crafting service. `craft(_:for:on:)` drains inputs from the combined inventory+warehouse pool (inventory first to free backpack slots, warehouse second), output goes to inventory. `CraftResult` enum (success / missingMaterials / inventoryFull / unknownRecipe / unknownItem) + `Shortage` struct for the modal alert. Post-drain slot accept-check (predicts slots freed by ingredient drain) so a craft never refuses spuriously when a 50/50 bag would have made room.
    │   ├── WeaponUpgradeService.swift # Phase 5.2.2: pure weapon-upgrade service. `upgrade(for:on:)` finds the equipped main-hand row, validates next tier exists in WeaponUpgradeCatalog, gates on user.estateLevel >= nextTier, snapshots inventory + warehouse availability for inputs, drains them (inventory first), bumps `InventoryEntry.tier` on the SAME row (item id never changes), runs EquipmentService.recomputeBonuses + saveAndCache. UpgradeResult enum: success(newTier) / maxTierReached / estateLevelTooLow(required, current) / missingMaterials([Shortage]) / noWeaponEquipped — every failure mode maps cleanly to a modal alert in the Workshop UI.
    │   ├── EstateUpgradeService.swift # Phase 5.3c (silver since 2026-05-18 rename): pure estate-tier upgrade service. `upgrade(for:on:)` checks in order: `EstateUpgradeCatalog.nextStep` exists (else maxTierReached) → `user.level >= requiredPlayerLevel` (else playerLevelTooLow) → `user.silver >= silverCost` (else **insufficientSilver**) → material snapshot for inputs (else missingMaterials) → drain materials (inventory first, warehouse for shortfall) → drain `user.silver` → bump `user.estateLevel` → saveAndCache. Result enum: success(newTier) / maxTierReached(tier) / playerLevelTooLow(required, current) / insufficientSilver(required, current) / missingMaterials([Shortage]). Silver check sits before materials so a cash-short player gets a clean "🪙 Not enough silvers" alert instead of a noisy materials breakdown.
    │   ├── BagUpgradeService.swift  # Phase 5.3d: pure bag-tier upgrade service mirroring WeaponUpgradeService / EstateUpgradeService. `upgrade(for:on:)` checks: `BagCatalog.nextStep` exists (else maxTierReached) → `user.estateLevel >= requiredEstateLevel` (else estateLevelTooLow) → material snapshot for inputs (else missingMaterials) → drain materials (inventory first, warehouse for shortfall) → bump `user.bagTier` → saveAndCache. Result enum: success(newTier, newCapacity) / maxTierReached(tier) / estateLevelTooLow(required, current) / missingMaterials([Shortage]). Materials only — no silver cost.
    │   ├── TraderService.swift      # Phase 6.1 (silver since 2026-05-18 rename): quantity-aware orchestrator for trader buy/sell. `sell(itemId, quantity, for:, on:)` validates bag has ≥ quantity, drains via `InventoryEntry.remove`, credits `quantity × sellPacketSilver`. `buy(itemId, quantity, for:, on:)` validates silver + slot space, debits `quantity × buyPacketSilver`, adds. Typed result enums (`SellResult.success(itemId,soldQty,silverGained) / .notEnoughInBag(have,need) / .unknownListing`; `BuyResult.success(itemId,boughtQty,silverSpent) / .notEnoughSilver(have,need) / .inventoryFull(free,need) / .unknownListing`) drive precise UI banners. Atomic preflight on buy so a failed transaction never leaves silver debited or items half-added. Called only from the `[✏️ N]` prompt flow now (×1 taps removed in the 2026-05-18 trader flatten).
    │   ├── FortuneService.swift     # Phase 6.4 (silver since 2026-05-18 rename): pure orchestrator for tarot draws. `draw(for:on:) -> DrawResult` checks cooldown via `user.lastFortuneDrawAt + 24h` (returns `.onCooldown(secondsLeft)` if still gated), checks silver (returns `.notEnoughSilver(have:need:)` if short of 10), debits the price, picks a card uniformly from `FortuneCatalog.all`, applies one-shot effects (silver delta with min-clamp to user.silver; Wheel 50-50 random; XP via `user.grantXP`; HP/Vigor restore to max), stamps `activeFortuneCardId` + `activeFortuneExpiresAt` (now + 6h buff window) + `lastFortuneDrawAt` (now, drives the 24h cooldown). Returns `.success(card, oneShotApplied)` with `OneShotApplied` snapshot (silverDelta after Wheel resolution, xp gained, hp/vigor restored flags) so the UI can render exact "+🪙 30 · balance 145" / "HP restored" / "+75 XP" lines without re-deriving.
    │   ├── TavernService.swift      # Phase 6.2 (silver since 2026-05-18 rename): pure orchestrator for tavern transactions. `buyDish(itemId, for:, on:)` mirrors the trader buy flow — atomic silver + slot preflight, debits, adds dish to bag. `BuyDishResult.success(silverSpent) / .notEnoughSilver(have,need) / .inventoryFull(free,need) / .unknownListing`. `resolveWager(playerScore, houseScore) -> WagerOutcome (.win / .lose / .tie)` is the pure score-comparison helper used by gambling — controller drives the animation flow (sendDice + sleeps) and calls this for the outcome bookkeeping. Win = +2× wager net (credit 2× wager since 1× was already debited at Roll tap); lose = wager kept by house; tie = refund 1× wager.
    │   ├── PlotService.swift        # Phase 5.1 + 2026-05-18 harvest-destination picker. Pure helpers `accumulated(for:)` / `bonusAccumulated(for:)` lazily compute primary + bonus yield from `lastHarvestedAt + ratePerSecond × elapsed` (capped). `harvest(_:to:for:on:)` takes a new `HarvestDestination` enum (`.bag` / `.warehouse`) — caller picks the destination via the picker UI in EstateController. New `HarvestResult.bagFull(primary:bonus:free:need:)` case is atomic — nothing moves and the plot timestamp is NOT reset, so the yield stays for retry. `.success(primary, bonus, destination)` reports which destination landed. Bag preflight uses `InventoryEntry.slotsUsed + total <= slotCap` (developer accounts bypass). `claim(slot:type:for:on:)` and the `slotsForLevel(_:)` table unchanged.
    │   └── TravelService.swift      # Phase 6.0: estate↔capital trip orchestrator. `start(user, destination, on, bot, lingo)` validates guards (`alreadyTraveling` / `onExpedition` / `dead` / `starving` / `alreadyAtDestination` via `StartFailure` enum), persists a `TravelState` row, and arms a `Task.detached` that sleeps until `endsAt`, then flips `user.location` + `user.routerName` (capital→capital ctrl, estate→main ctrl), deletes the trip row, and pushes the arrival message with the destination's reply-keyboard. `rescheduleInflight(on:bot:lingo:)` on bot startup re-arms every in-flight trip — sleeps elapsed during downtime fire immediately. `testMode = true` (2 seconds per minute → 2-second trip); flip to false before shipping. `travelMinutes = 2` constant; `formatCountdown(_)` mirrors `PassiveExpeditionService.formatCountdown` for MM:SS rendering.
    │
    ├── Telegram/
    │   ├── Router/
    │   │   ├── Router.swift        # Path-matching engine (command, content type, callback)
    │   │   ├── Context.swift       # Request context: bot, db, lingo, update, session, args
    │   │   ├── Command.swift       # Command name matcher (slash handling, case sensitivity)
    │   │   ├── Commands.swift      # Commands enum (start, cancel, exit, settings, language, profile, explore, estate, capital, inventory)
    │   │   ├── ContentType.swift   # Enum of all matchable Telegram content types
    │   │   ├── Arguments.swift     # Scanner-based argument parser (words, ints, doubles)
    │   │   └── Router+Helpers.swift # Subscript shortcuts for adding handlers
    │   │
    │   └── TGBot/
    │       ├── TGDispatcher.swift   # Main dispatcher: auth + global cmds + router catch-all
    │       └── HummingbirdTGClient.swift # TGClientPrtcl impl using AsyncHTTPClient
    │
    └── Helpers/
        ├── TGBot+Extensions.swift  # TGControllerBase (+ dismissPendingPicker, **postStatusBanner** — 2026-05-15: sends `✅ ...` / `❌ ...` status as a standalone message under the inline-kb screen and deletes the previous banner so chat history stays clean), Context.session, TGMessage helpers
        ├── SessionCache.swift      # Actor-based user cache (5min TTL, auto-cleanup)
        ├── Lingo+Locales.swift     # Lingo convenience: accept SupportedLocale enum
        ├── EphemeralChatState.swift # Actor — in-memory cache of transient state. `pendingPickers` (mode-picker → auto-delete on navigation). **2026-05-15** `pendingWarehouseTransfers`: PendingWarehouseTransfer (itemId / promptMessageId / warehouseMessageId / direction: .put|.take|nil) for warehouse `[✏️ N]` flow. `lastStatusBanners`: latest standalone status-banner message ID per user — drives `TGControllerBase.postStatusBanner` so each new banner deletes its predecessor. **2026-05-17 (Phase 6.1)** `pendingTraderTransfers`: PendingTraderTransfer (itemId / direction: .buy|.sell / promptMessageId / traderScreenMessageId / isPhoto) — set by the trader's `[✏️ N]` callback, consumed by `CapitalController.unmatched` on the next text update; same invalid-input-keeps-pending / validation-failure-clears semantics as the warehouse flow. **2026-05-28 (Phase 6.5)** `pendingMarketListings`: PendingMarketListing (itemId / stage: .quantity|.price / quantity / promptMessageId / marketScreenMessageId / isPhoto) for the two-stage sell flow. **2026-06-10 (Phase 6.5)** `pendingTradeInputs`: PendingTradeInput (kind: .silver|.itemQty(itemId) / promptMessageId / screenMessageId / isPhoto) for the trade silver/qty prompts. Both consumed by `CapitalController.unmatched`; their prompt messages are deleted on submit/cancel (2026-06-15 visibility policy). **2026-06-16 (Phase 7.1)** `pendingGuildInputs`: PendingGuildInput (kind: .foundName | .inviteName | .vaultDeposit(itemId) | .vaultWithdraw(itemId) | .treasuryDeposit | .treasuryWithdraw / promptMessageId) — consumed by `GuildController.unmatched`, dispatched by kind. The earlier `pendingTavernGames` entry (player-rolls-own-dice state machine) was added then removed when gambling pivoted to the bot-rolls-all model. (The `lastSceneryPhotos` slot added 2026-05-17 for scenery-photo replacement was removed 2026-05-20 when location photos became permanent chat history — see `PhotoCache.swift`.)
        ├── PhotoCache.swift         # **2026-05-17, reworked 2026-05-20** — actor cache `[assetPath: fileId]` + top-level `sendCachedPhoto(assetPath:caption:parseMode:replyMarkup:toUser:bot:)` helper. After the first `bot.sendPhoto` for a given asset, Telegram returns a `file_id` that the bot caches and reuses on all later sends — no repeat JPG/PNG uploads. **No deletion** — photos stay in chat history (players wanted a scrollable record of visits; file_id dedup means a long history of repeated backdrops costs no extra storage). Falls back to `bot.sendMessage` with the caption when the asset is missing entirely. **This is the default photo send-path for ALL player-visible art** — location backdrops AND registration/lore scenes (the latter converted from direct `bot.sendPhoto(.file)` on 2026-05-20). The 2026-05-20 rework renamed `sendScenicPhoto`→`sendCachedPhoto` and dropped the old scenery-slot auto-deletion.
        ├── GameDay.swift            # 2026-07-28: the shared daily-reset boundary — rolls at **12:00 Kyiv**, not midnight (an evening session plus next morning spans one in-game day). `GameDay.stamp(date)` → `yyyy-MM-dd` key of the game day an instant falls in (shifts back by `rolloverHour`, then formats in Europe/Kyiv). Used by `ArenaProfile` (daily fight budget) and `QuestProgress` (daily job). `secondsUntilNextRollover(from:)` (calendar search in the Kyiv zone, DST-safe) powers the journal's countdown.
        ├── DotEnv+Env.swift        # Env helper: get env vars with fallback to .env file
        ├── ContentBootstrap.swift  # **2026-08-29 (rebalance Phase 0)** — parse → validate → build → install seam for `content/data`. NOT called yet; wires into `configure` in Phase 2, after `Dotenv.configure` and BEFORE the DB block (the dev-seed later in `configure` already calls `ItemCatalog.find`). Deliberately carries NO `import ROIContent` — it is the live proof that the `@_exported import` in configure.swift propagates.
        └── ContentMapping.swift    # **2026-08-29 (Phase 1)** — domain ⇄ DTO in BOTH directions for Item/Enemy/Recipe/WeaponLadder. `toDomain()` exists now because the round-trip proof is meaningless unless the DTO can rebuild the domain value; Phase 2 reuses it verbatim. Locale keys are omitted when they equal the derived form (`item.<id>`, `<nameKey>.desc`, the enemy's own id) — verified to hold for all 33 items and all 9 enemies.
        ├── ContentExporter.swift   # **2026-08-29 (Phase 1, narrowed in Phase 2)** — `--export-content` migration tool. Exports ONLY catalogs that are still Swift arrays; Item/Enemy/Recipe were removed once they became façades, because re-exporting them would write back what was just loaded and a self-check over that circle proves nothing. Currently exports weapon_upgrades only; Phase 3 adds the remaining 11. Three verification layers, of which **Layer 0 (domain equivalence) is load-bearing**: the canonical round-trip stays byte-stable even when a field is never captured, so only comparing rebuilt-vs-original fingerprints catches a dropped column (proven — dropping `teachesRecipe` left Layer 1 green, Layer 0 red). DELETE at end of Phase 3.
        ├── ContentDigest.swift     # **2026-08-29 (Phase 2)** — `--content-digest`, verification layer 3. Two halves: field-complete record fingerprints in catalog order, and a **seeded `pickFor` replay** (40 depths × 200 draws, fixed seed). The second half is what catches a change in SELECTION LOGIC that record fingerprints cannot see — proven by negative test: dropping `?? all.first` left `records` byte-identical and moved `spawns`. Also runs a live façade smoke test (`find()` on every recipe input/output, loot id, scroll, starter recipe and class starter weapon). Baseline `545017168ce60953` matched before and after the Phase 2 flip.
        ├── Catalogs.swift          # **2026-08-29 (Phase 2)** — `DomainContent` + `Catalogs` holder: the live DOMAIN-side snapshot the three façades read. Exists next to `GameData` (DTO snapshot) because the domain types still live in the main target, and mapping DTO→domain per `find()` would allocate inside combat loops. `ContentBootstrap.load` is the ONLY installer of either and installs both from one bundle so they cannot drift. `nonisolated(unsafe)` + `NSLock`, same shape as `GameData`.
```
